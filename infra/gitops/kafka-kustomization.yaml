apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: kafka
  namespace: flux-system
spec:
  dependsOn:
  - name: platform
  interval: 10m0s
  path: ./infra/platform/kafka
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  wait: true
